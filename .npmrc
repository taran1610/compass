# @langchain/community pulls optional peers (e.g. @browserbasehq/stagehand) that require zod@^3;
# this app uses zod@^4 for the AI SDK. Peer resolution still works at runtime for SupabaseVectorStore.
legacy-peer-deps=true
